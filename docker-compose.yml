version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
      - PORT=5000
    command: gunicorn --bind 0.0.0.0:5000 app:app
